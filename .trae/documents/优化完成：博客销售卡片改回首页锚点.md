我已成功修改 `build.py` 并重新生成了站点，将所有博客子文章（Sub-articles）中的销售卡片外链替换为了指向首页对应版块的锚点链接。这将有效防止权重从内容页流失到外部支付页。

### 修改内容
1.  **修改 `build.py`**:
    *   更新了 `fix_links` 函数，增加了 `replace_sales_links` 参数。
    *   定义了销售链接映射规则：
        *   `/go/gpt-shared` -> `/#pricing-low` (指向首页共享号版块)
        *   `/go/gpt-topup` -> `/#pricing-high` (指向首页 Plus 代充版块)
        *   `/go/gpt-exclusive` -> `/#pricing` (指向首页独享号版块)
    *   在构建博客文章（`main` 函数循环）时，启用了此替换逻辑。

### 验证结果
运行 `audit.py` 后的最新审计报告显示：
*   **Outbound External Links (外链数量)**:
    *   **博客文章页**: 全部降为 **0** (之前为 3)。这意味着权重不再流失。
    *   **首页 (`index.html`)**: 保持为 **3** (正常，首页承担转化入口)。
*   **Redirect Sources**:
    *   日志显示跳转外链的来源现在仅剩 `index.html`。

### 结论
修改已生效，您的博客页面现在通过内链（锚点）引导用户回到首页进行转化，既保留了 SEO 权重，又优化了用户体验。