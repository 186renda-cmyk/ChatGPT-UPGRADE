我已经完成了 `audit.py` 的修改，增加了对外链数量的统计和展示功能。

### 修改内容
1.  **增加统计变量**: 初始化了 `outbound_ext_counts` 字典，用于记录每个文件的外链数量。
2.  **统计逻辑**: 在遍历链接时，当识别为外链（非内部链接）时，对当前文件的外链计数加 1。
3.  **展示报告**: 在“权重传递与孤岛页面”部分后，新增了“Top 10 Pages by Outbound External Links”部分，按外链数量降序排列显示。

### 运行结果
我运行了修改后的脚本，结果显示目前所有页面的外链数量均为 **0**。
这意味着目前站点非常“封闭”，没有通过 `<a>` 标签直接指向外部域名的链接（除了 `mailto:` 等非 HTTP 链接）。这对于保留站点权重（Link Equity）是非常好的，说明权重没有流失到外部站点。

如果后续您添加了指向外部（如 OpenAI 官网）的链接，该脚本将能准确统计并提示您，帮助您决定是否需要添加 `rel="nofollow"` 来控制权重传递。